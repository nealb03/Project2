name: Start Project1 (EC2 + Backend)

on:
  workflow_dispatch:
    inputs:
      aws_region:
        description: "AWS Region"
        required: true
        default: "us-east-1"
      instance_id:
        description: "EC2 instance id"
        required: true
        default: "i-0aa128f11e1299832"
      port:
        description: "Backend port"
        required: true
        default: "5001"

permissions:
  id-token: write
  contents: read

jobs:
  start:
    runs-on: ubuntu-latest

    steps:
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ inputs.aws_region }}
          role-to-assume: arn:aws:iam::288761763536:role/GithubActionsRole

      - name: Start EC2 and wait until running
        shell: bash
        run: |
          set -euo pipefail
          aws ec2 start-instances --instance-ids "${{ inputs.instance_id }}" --output table
          aws ec2 wait instance-running --instance-ids "${{ inputs.instance_id }}"

      - name: Wait for SSM agent Online
        shell: bash
        run: |
          set -euo pipefail
          INSTANCE_ID="${{ inputs.instance_id }}"

          echo "Waiting for SSM Online for $INSTANCE_ID ..."
          for i in $(seq 1 90); do
            STATUS="$(aws ssm describe-instance-information \
              --filters "Key=InstanceIds,Values=$INSTANCE_ID" \
              --query "InstanceInformationList[0].PingStatus" \
              --output text 2>/dev/null || true)"

            echo "SSM PingStatus: ${STATUS:-<none>}"
            if [ "$STATUS" = "Online" ]; then
              echo "SSM is Online."
              exit 0
            fi
            sleep 10
          done

          echo "SSM did not become Online in time."
          exit 1

      - name: Start Backend via SSM (detached) + show SSM output
        shell: bash
        run: |
          set -euo pipefail
          INSTANCE_ID="${{ inputs.instance_id }}"
          PORT="${{ inputs.port }}"

          cat > start-backend.ps1 <<'PS1'
          $ErrorActionPreference = "Stop"
          $port = "__PORT__"

          # From your screenshot:
          $apiDir = "C:\Backend01\MyWebApi"
          if (!(Test-Path $apiDir)) { throw "API_DIR not found: $apiDir" }

          # If your csproj name differs, change it here:
          $csproj = Join-Path $apiDir "MyWebApi.csproj"
          if (!(Test-Path $csproj)) { throw "CSPROJ not found: $csproj" }

          $logDir = "C:\Apps\logs"
          New-Item -ItemType Directory -Force -Path $logDir | Out-Null

          $pidFile = Join-Path $logDir "api.pid"
          if (Test-Path $pidFile) {
            try {
              $oldPid = (Get-Content $pidFile -ErrorAction SilentlyContinue)
              if ($oldPid) { Stop-Process -Id $oldPid -Force -ErrorAction SilentlyContinue }
              Remove-Item $pidFile -Force -ErrorAction SilentlyContinue
            } catch {}
          }

          $ts = Get-Date -Format "yyyyMMdd-HHmmss"
          $outLog = Join-Path $logDir ("api-out-" + $ts + ".txt")
          $errLog = Join-Path $logDir ("api-err-" + $ts + ".txt")

          $dotnet = (Get-Command dotnet -ErrorAction SilentlyContinue).Source
          if (!$dotnet) {
            $fallback = "C:\Program Files\dotnet\dotnet.exe"
            if (Test-Path $fallback) { $dotnet = $fallback } else { throw "dotnet not found (PATH and $fallback missing)" }
          }

          $argList = @(
            "run",
            "--project", "`"$csproj`"",
            "--urls", "http://0.0.0.0:$port"
          ) -join " "

          $p = Start-Process -FilePath $dotnet `
            -ArgumentList $argList `
            -WorkingDirectory $apiDir `
            -WindowStyle Hidden `
            -RedirectStandardOutput $outLog `
            -RedirectStandardError $errLog `
            -PassThru

          $p.Id | Out-File -FilePath $pidFile -Force

          Start-Sleep -Seconds 3

          # verify alive + verify listening
          try { $null = Get-Process -Id $p.Id -ErrorAction Stop } catch { throw "Backend exited immediately. Check $errLog and $outLog" }

          $listen = netstat -ano | Select-String -Pattern (":$port\s+LISTENING")
          if (!$listen) { throw "Started PID=$($p.Id) but port $port is not LISTENING. Check $errLog and $outLog" }

          "OK: Started PID=$($p.Id) listening on $port" | Out-File -FilePath $outLog -Append
          PS1

          sed -i "s/__PORT__/${PORT}/g" start-backend.ps1
          B64="$(base64 -w 0 start-backend.ps1)"

          COMMAND_ID="$(aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunPowerShellScript" \
            --comment "Start backend detached + verify" \
            --parameters "{\"commands\":[\"[Text.Encoding]::UTF8.GetString([Convert]::FromBase64String('$B64')) | Set-Content -Path C:\\\\Windows\\\\Temp\\\\start-backend.ps1 -Encoding UTF8\",\"powershell.exe -NoProfile -ExecutionPolicy Bypass -File C:\\\\Windows\\\\Temp\\\\start-backend.ps1\"]}" \
            --query "Command.CommandId" \
            --output text)"

          echo "SSM CommandId: $COMMAND_ID"

          # give it longer than default waiter
          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" \
            --max-attempts 120 \
            --delay 5

          echo "---- SSM invocation status ----"
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" \
            --query "{Status:Status,StatusDetails:StatusDetails,ResponseCode:ResponseCode}" \
            --output table

          echo "---- SSM STDOUT ----"
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" \
            --query "StandardOutputContent" \
            --output text || true

          echo "---- SSM STDERR ----"
          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" \
            --query "StandardErrorContent" \
            --output text || true