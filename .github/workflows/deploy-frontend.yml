name: Build and Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'Frontend01/**'                             # only run when Frontend01 changes
      - '.github/workflows/deploy-frontend.yml'     # or when this workflow itself changes
  workflow_dispatch:

env:
  AWS_REGION: us-east-1                 # change if your region is different
  ECS_CLUSTER: your-ecs-cluster-name    # TODO: update to your ECS cluster
  FRONTEND_SERVICE: your-frontend-service-name  # TODO: update to your ECS service

jobs:
  build-and-deploy-frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'   # or whatever version your FE uses

      - name: Install dependencies
        working-directory: Frontend01
        run: npm install

      - name: Build frontend
        working-directory: Frontend01
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # Example: build & push Docker image to ECR (if you containerize frontend)
      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v2
      #
      # - name: Build, tag, and push image to ECR
      #   env:
      #     ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      #     ECR_REPOSITORY: your-frontend-ecr-repo
      #     IMAGE_TAG: ${{ github.sha }}
      #   run: |
      #     docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG Frontend01
      #     docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
      #
      # - name: Deploy to ECS
      #   uses: aws-actions/amazon-ecs-deploy-task-definition@v1
      #   with:
      #     task-definition: path/to/your-task-def.json
      #     service: ${{ env.FRONTEND_SERVICE }}
      #     cluster: ${{ env.ECS_CLUSTER }}
      #     wait-for-service-stability: true
