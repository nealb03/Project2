name: Deploy Backend Fargate CI/CD Deployment to AWS

on:
  push:
    branches:
      - main
      - terraform-modules
    paths:
      - 'App2-Fargate/Backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:
    inputs:
      apply:
        description: 'Set to true to apply deployment'
        required: false
        default: 'false'

env:
  AWS_REGION: us-east-1
  ECS_CLUSTER: brentaneal-cluster
  BACKEND_SERVICE: brentaneal-backend-service

jobs:
  build-and-deploy-backend:
    name: Build and Deploy Backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Build backend
        working-directory: App2-Fargate/Backend
        run: |
          echo "Building App2 Fargate backend..."
          # TODO: uncomment and adapt for your stack:
          # npm install
          # npm run build
          # mvn clean package
          # dotnet build

      - name: Deploy backend (Conditional Apply)
        if: github.event.inputs.apply == 'true'
        working-directory: App2-Fargate/Backend
        run: |
          echo "Deploying App2 Fargate backend..."
          # TODO: replace with your real deployment commands, e.g.:

          # For ECS:
          # aws ecs update-service \
          #   --cluster ${{ env.ECS_CLUSTER }} \
          #   --service ${{ env.BACKEND_SERVICE }} \
          #   --force-new-deployment

          # For Lambda:
          # zip -r deployment.zip .
          # aws lambda update-function-code \
          #   --function-name your-function \
          #   --zip-file fileb://deployment.zip

          # Or call a deployment script, e.g.:
          # ./deploy-backend.sh
